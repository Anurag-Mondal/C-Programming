// # is called preprocessor & #include is called
// preprocessor directive.
#include <stdio.h> // stdio.h is a header file (library of functions)
#include <limits.h>
#include <math.h>
int main () {
    int x = INT_MAX;
    int y = INT_MIN;
    long z = LONG_MAX;
    long a = LONG_MIN;
    printf ("%ld",z);
}