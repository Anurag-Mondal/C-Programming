#include <stdio.h>
int main () {

    // int i = 65;
    // char ch = (char)i;       // This makes 'i' a character
    // printf ("%c",ch);

    char ch  = 'a';
    int i = (int)ch;       // This makes 'ch' a number
    printf ("%d",i);
    return 0;
}